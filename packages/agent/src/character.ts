import characterJson from "@characters/doolittle.character.json";
import { createCharacter } from "@elizaos/core";

export const character = createCharacter(characterJson);

export default character;
