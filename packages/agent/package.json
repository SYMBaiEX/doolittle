{
  "name": "@eliza-agent/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Primary Eliza Agent application package.",
  "dependencies": {
    "@eliza-agent/acp": "workspace:*"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --target bun --outdir ../../dist --external sharp"
  }
}
