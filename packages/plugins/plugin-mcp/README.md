# MCP Plugin for ElizaOS (vendored workspace package)

Model Context Protocol integration plugin for ElizaOS.

This workspace copy is vendored into `packages/plugins/plugin-mcp` and normalized for the current ElizaOS alpha line used by Eliza Agent.

## Provenance

- Upstream package name: `@elizaos/plugin-mcp`
- Workspace location: `packages/plugins/plugin-mcp`
- Runtime line: current ElizaOS alpha track

## Contents

- source-first workspace entrypoint at `src/index.ts`
- local package manifest normalized for the workspace
- MCP service wrapper aligned with the sibling vendored plugin conventions
