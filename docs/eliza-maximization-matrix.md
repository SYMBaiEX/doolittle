# Eliza Maximization Matrix

Last updated: August 10, 2026

> Maintainer audit: this is a dated ownership review, not generated runtime
> truth. Use the [plugin inventory](./plugin-inventory.md) and [capability
> truth](./capability-truth.md) for current assembled behavior.

This matrix tracks where Doolittle is aligned with the ElizaOS beta-targeted
runtime stack and where native service ownership can still increase.

The verified runtime target remains `2.0.3-beta.7`. Do not treat the
semver-higher `2.0.11-beta.7` artifacts as an upgrade: they do not map to the
later beta.7 source tag and include an agent-orchestrator package without
runtime code.

## Foundation

| Subsystem | Current Eliza usage | Not native enough yet | Next package or service to push |
|---|---|---|---|
| Runtime core | `@elizaos/core` `AgentRuntime` + `DefaultMessageService`, `@elizaos/agent`, and `@elizaos/skills`; Doolittle explicitly enables native documents, relationships, trajectories, extended capabilities, and secrets, and registers the independently importable beta.7 foundation classes for permissions, pending prompts, global pause, handoff, character persistence, local files, media generation, knowledge, hooks, approvals, pairing, tool policy, plugin management, and agent events | The product still owns the outer CLI/API/desktop composition because the aggregate `runtime/eliza-plugin` also pulls routes, actions, trigger/media init hooks, notifications, and an unavailable relationships-graph-builder import | Keep the product composition thin; reference public service identifiers instead of parallel string constants, add only independently importable service classes with focused start/stop proof, and adopt aggregate-plugin behavior only when its full dependency and lifecycle surface is desktop-safe |
| Native plugin assembly | [`packages/agent/src/runtime/native/plugin-registry/index.ts`](../packages/agent/src/runtime/native/plugin-registry/index.ts), `@elizaos/agent/services/registry-client` | Some plugin inventory and service ownership still flow through product summaries | Use exported registry/plugin-manager contracts before adding direct plugin-manager dependencies |
| Package ownership | Official packages use the reserved `@elizaos/*` namespace; Codex now comes directly from `@elizaos/plugin-codex-cli`, while Doolittle-only Claude Code, Devin, provider-transport, and SQL relationship projection packages use `@doolittle/*` | Five unpublished beta resolver shims remain in `@elizaos/*` and are individually declared by the SDK release gate | Replace each resolver shim when its exact beta package becomes public; never add an undeclared local package under the official namespace |
| Agent SDK usage | [`packages/agent/src/runtime/native/agent-sdk.ts`](../packages/agent/src/runtime/native/agent-sdk.ts) and [`packages/agent/src/services/agent-sdk-service.ts`](../packages/agent/src/services/agent-sdk-service.ts) own package compatibility and plugin-registry inspection; skill catalog state is excluded and comes from the official Agent Skills service projection | Product summaries still project SDK package and registry state for operator UX, but do not own their lifecycle | Continue consuming exported `@elizaos/agent` compatibility and registry contracts without broadening this facade into another lifecycle owner |
| Desktop client | Official `@elizaos/ui` `ElizaClient` and `AgentRequestTransport` own ordinary renderer request construction, client identity, timeouts, resuming retries, response parsing, and `ApiError` semantics through [`apps/desktop/src/renderer/eliza-client.ts`](../apps/desktop/src/renderer/eliza-client.ts) | Electron must serialize `Request` and `Response` data across context-isolated IPC, and the main process retains the product route/query/header allowlist; token and terminal streams use dedicated cancellable IPC channels | Keep the adapter structural and streaming-free, use official domain client methods when their routes match Doolittle's public API, and never restore a second renderer fetch/error stack |
| Media generation and transcription | Official `AgentMediaGenerationService`, `ModelType.IMAGE`, `ModelType.TEXT_TO_SPEECH`, `ModelType.TRANSCRIPTION`, and core `fetchRemoteMedia` own provider selection, inference, and guarded remote artifact download | Doolittle retains workspace-safe local artifacts, prompts, manifests, reports, sidecar discovery, and truthful SVG/offline fallbacks; it no longer calls OpenAI or FAL transports directly | Keep provider credentials and transports inside registered Eliza plugins, and derive readiness from the runtime model registry rather than environment-key guesses |
| Secrets | The core `SECRETS` capability owns encrypted global, world, and user secrets, validation, access logs, setup providers, plugin activation, and the conversational `SECRETS` action. [`packages/plugins/doolittle-plugin/secrets-vault`](../packages/plugins/doolittle-plugin/secrets-vault) is only the namespaced durable global-secret mirror over official `@elizaos/vault` | Global character settings are not independently durable in the pinned SDK, so Doolittle hydrates and mirrors only that storage level; world and user levels already use Eliza database storage | Keep API and runtime consumers on `SECRETS`, preserve `ENCRYPTION_SALT`, and remove the Vault mirror when upstream global storage becomes independently durable |

## Knowledge and Identity

| Subsystem | Current Eliza usage | Not native enough yet | Next package or service to push |
|---|---|---|---|
| Knowledge ingestion | The registered `@elizaos/plugin-pdf` `pdf` service owns PDF conversion; the official `KnowledgeGraphService` owns entity and relationship knowledge; [`packages/agent/src/services/documents-service.ts`](../packages/agent/src/services/documents-service.ts) only resolves workspace paths/base64 and projects extraction into product UX | The official graph is intentionally not treated as a replacement for Doolittle's bounded shared `MEMORY.md`; user-specific durable memory is canonical in the Eliza rolodex profile | Keep PDF extraction and entity relationships service-native while preserving explicit shared-memory semantics as product behavior |
| Gateway pairing | The official Eliza `PairingService` owns request TTLs, collision-safe codes, allowlists, and database persistence; [`packages/agent/src/services/gateway-pairing.ts`](../packages/agent/src/services/gateway-pairing.ts) only projects SDK records into gateway/API contracts and migrates the retired JSON store once | Doolittle retains platform policy (`allow`, `deny`, or `pair`) and presentation, but no longer owns a parallel pairing database | Keep channel plugins and desktop controls on the shared SDK pairing lifecycle |
| Embeddings | `@elizaos/plugin-ollama` and the official `@elizaos/plugin-elizacloud` own local and managed-cloud embedding handlers | Doolittle only selects the provider and may disable cloud embeddings when local embedding ownership is configured | Keep model generation inside official provider packages and retain only selection policy in Doolittle |
| Codex inference | `@elizaos/plugin-codex-cli` owns Codex OAuth-file reads, locked atomic refresh, Responses API translation, streaming, structured output, and native tool calls | Doolittle retains provider selection and secret-free account health plus the separate multi-account subprocess pool | Keep the official plugin as the only in-process Codex model transport; do not restore a Doolittle Codex HTTP/SSE bridge |
| Personality | [`packages/plugins/doolittle-plugin/identity/personality`](../packages/plugins/doolittle-plugin/identity/personality), [`packages/agent/src/services/personality-service.ts`](../packages/agent/src/services/personality-service.ts) | Doolittle keeps product-specific personality behavior while exposing it as an Eliza service | Doolittle identity facet inside the consolidated product plugin; upstream only when ElizaOS publishes a matching service contract |
| User profiles and memory | [`packages/plugins/doolittle-plugin/identity/rolodex`](../packages/plugins/doolittle-plugin/identity/rolodex), [`packages/agent/src/services/user-profile/service/index.ts`](../packages/agent/src/services/user-profile/service/index.ts), [`packages/agent/src/services/experience-memory-service.ts`](../packages/agent/src/services/experience-memory-service.ts) | The Eliza rolodex profile is the single user-memory owner; the Doolittle memory surface is a compatibility projection and one-time `USER.md` importer | Keep the native rolodex boundary canonical and remove compatibility parsing only when every client uses profile APIs directly |
| Session and experience | Eliza rooms and `messages` memories are canonical for every user and assistant turn; [`packages/agent/src/runtime/chat-turn/conversation-persistence.ts`](../packages/agent/src/runtime/chat-turn/conversation-persistence.ts) projects native memories into [`packages/agent/src/services/session/service/index.ts`](../packages/agent/src/services/session/service/index.ts) for synchronous desktop/CLI history. Advanced memory implements the official `MemoryStorageProvider`, `LongTermMemory`, and `SessionSummary` contracts | Doolittle still owns the SQL implementation plus project membership, titles, forks, archive/import metadata, FTS, and usage views because those are product persistence semantics rather than a second chat lifecycle | Keep Eliza memory contracts authoritative and keep the local session database limited to the storage adapter and product read model |

## Execution and Orchestration

| Subsystem | Current Eliza usage | Not native enough yet | Next package or service to push |
|---|---|---|---|
| Shell and execution | Eliza action contracts and `ActionResult`, `@elizaos/agent/services/sandbox-manager`, and the namespaced `doolittle_shell` product projection over [`packages/agent/src/services/terminal/service.ts`](../packages/agent/src/services/terminal/service.ts); the retired unnamespaced resolver has been removed | The installed official shell plugin owns the `shell` service identifier but its API is not shape-compatible with Doolittle's terminal history, status, and desktop PTY projection | Adapt product policy to the official service before enabling it; never register or resolve a Doolittle projection under an official service identifier |
| Native autonomy | Core `autonomyCapabilities` supplies the official `AutonomyService`, enable/disable/escalate actions, status providers, and `/autonomy/*` routes. Doolittle mounts that complete public surface in the foundation plugin and exposes its status, cadence, and explicit enable/disable controls in the desktop Runtime page | The native reasoning loop is disabled by default because enabling it can consume provider tokens in the background. Doolittle's separate autonomy profile remains operator policy, not a replacement service | Keep lifecycle, prompt batching, reasoning, and action execution inside the official service; retain only the trusted desktop route allowlist and concise operator controls |
| Coding agent | [`packages/plugins/doolittle-plugin/coding-agent`](../packages/plugins/doolittle-plugin/coding-agent), native action/provider/service registration; workspace and repository actions plus operator commands resolve the hot `doolittle_coding_agent` service exclusively | `@elizaos/plugin-coding-tools` beta.7 was evaluated but its direct file writes bypass Doolittle's Git checkpoint and protected-uncommitted-file policy, while worktree cleanup can force-remove a worktree. Those are regressions, not equivalent native behavior | Retain the namespaced Doolittle coding adapter until the official plugin can enforce the same path, approval, checkpoint, and cleanup guarantees; do not mount both mutation owners |
| Conversational forms | `@elizaos/plugin-form` owns the `FORM` service, component-backed sessions/submissions, evaluator/provider flow, and the separately exported `FORM` restore action. [`packages/plugins/doolittle-plugin/forms`](../packages/plugins/doolittle-plugin/forms) registers Doolittle's default templates and retains its existing operator/autocoder record projection | Existing structured records remain in `forms-store.json` for compatibility and product reporting; they are not a second conversational session engine | Start new conversational flows through `FORM`, keep the local store as a bounded projection, and migrate callers when native submissions expose every required product receipt |
| GitHub | `@elizaos/plugin-github` owns the reserved `github` service and native pull-request, issue, comment, label, assignment, and notification actions with confirmation gates. Doolittle's create/delete repository planner is isolated as `doolittle_github_planning` | Repository creation/deletion remains deliberately plan-only product behavior and is not implemented by the native plugin | Keep native GitHub operations on the official service/actions and retain the namespaced planner only for capabilities with no official equivalent |
| Orchestrator | [`@elizaos/plugin-agent-orchestrator`](https://www.npmjs.com/package/@elizaos/plugin-agent-orchestrator), [`packages/agent/src/services/delegation/projection.ts`](../packages/agent/src/services/delegation/projection.ts) | Official plugin owns the native service boundary; Doolittle retains product delegation and supervision UX | Extend through the official service contract rather than restoring the removed local shadow |
| Scheduling | `@elizaos/agent/triggers/runtime`, `@elizaos/agent/triggers/scheduling`, shared automation contracts, and [`packages/agent/src/runtime/native/plugin-registry/product/trigger-runtime-service.ts`](../packages/agent/src/runtime/native/plugin-registry/product/trigger-runtime-service.ts) | Trigger Tasks are canonical; Doolittle owns only the operator-friendly automation projection, conditions, actions, and receipts | Keep product automation UX projected onto SDK tasks and upstream reusable receipt fields when available |

## Browser, MCP, and Research

| Subsystem | Current Eliza usage | Not native enough yet | Next package or service to push |
|---|---|---|---|
| Browser | `@elizaos/plugin-browser` owns the `browser` service and target dispatch. Doolittle registers an explicitly selected `doolittle-evidence` target, while the namespaced `doolittle_browser` facade preserves capture, comparison, analysis, and degraded artifact receipts over [`packages/agent/src/services/web/service.ts`](../packages/agent/src/services/web/service.ts). Basic HTTP fetches use core `fetchWithSsrfGuard`; only the exact `localhost` hostname receives the local-preview exception | The official browser command result does not represent Doolittle's full evidence bundle, artifact paths, or comparison analysis | Keep lifecycle, target routing, and network guarding native; retain only the evidence/artifact policy in the product target and facade |
| MCP | `@elizaos/plugin-mcp@2.0.3-beta.7` owns the `mcp` service, validated stdio/HTTP/SSE connections, retries, resources, discovery, provider context, the `MCP` action, and tool invocation. [`packages/agent/src/services/mcp/service.ts`](../packages/agent/src/services/mcp/service.ts) is now a thin, runtime-bound projection for Doolittle's CLI/API/desktop tool browser; marketplace lookup stays on official agent helpers | Doolittle retains only operator timestamps, server-qualified tool names, search/description formatting, and a legacy one-time command-to-`settings.mcp.servers` migration | Keep transport, lifecycle, validation, resource reads, provider context, and calls on the official service; do not restore subprocess execution or output parsing |
| Skill synthesis and catalog | `@elizaos/skills` and the official agent-skills service own loaded inventory, catalog search, details, sync, installation, and uninstallation; [`packages/agent/src/services/skills/service.ts`](../packages/agent/src/services/skills/service.ts) is limited to workspace metadata and startup projection, while the skills hub receives read-only official-service projections | Generated-skill families, imported manifests, and portable desktop bundles remain product distribution views rather than a second catalog authority | Keep native skill manifests canonical, keep catalog operations on `AGENT_SKILLS_SERVICE`, and do not restore a product catalog cache/client |
| Trajectories | `@elizaos/core` trajectory context/logger APIs, `@elizaos/agent/runtime/trajectory-persistence`, [`packages/agent/src/services/trajectory/sdk-native.ts`](../packages/agent/src/services/trajectory/sdk-native.ts), [`packages/agent/src/services/trajectory/service/index.ts`](../packages/agent/src/services/trajectory/service/index.ts) | The SDK `trajectories` lifecycle exclusively owns canonical model/tool traces; Doolittle exposes a distinctly named `trajectoryEvaluation` projection for replay, benchmarks, and non-training debug bundles | Keep the lifecycle/projection boundary guarded while enriching evaluation receipts from SDK trajectory identifiers |
| Logging | `@elizaos/logger` for agent, runtime, and desktop renderer emission; [`packages/agent/src/services/logger-service/index.ts`](../packages/agent/src/services/logger-service/index.ts) | The former `@doolittle/logger` package is retired. The remaining service only projects official log calls into the durable `/logs` and crash-path product contracts that the SDK does not expose as structured records | Keep direct console diagnostics out of application code and treat the projection as a compatibility boundary, not a second logging engine |
| Eliza Cloud | Official `@elizaos/plugin-elizacloud@2.0.3-beta.7` owns managed text, research, embeddings, image, speech, status, registry, billing, and cloud services | Doolittle retains account binding, active-provider selection, and operator presentation; the retired workspace shadow package and its direct HTTP client are gone | Consume new Cloud capabilities from the official plugin's public models, services, providers, and routes instead of recreating requests locally |

## Messaging and Control Plane

| Subsystem | Current Eliza usage | Not native enough yet | Next package or service to push |
|---|---|---|---|
| Telegram | `@elizaos/plugin-telegram` owns bot startup, inbound processing, rooms/entities, retries, chunking, message persistence, normal sends, and edits; [`packages/agent/src/gateway/platforms/telegram-adapter/index.ts`](../packages/agent/src/gateway/platforms/telegram-adapter/index.ts) resolves the live `telegram` service and projects its deliveries into the Doolittle journal | Doolittle retains gateway routing, delivery receipts, health presentation, and the voice-note product hint; it no longer owns a parallel raw Bot API sender | Keep outbound behavior on the native `telegram` service and keep the gateway adapter limited to routing and product receipts |
| Discord | `@elizaos/plugin-discord` registers the official message connector. [`packages/agent/src/gateway/platforms/native-message-connector-adapter.ts`](../packages/agent/src/gateway/platforms/native-message-connector-adapter.ts) maps Doolittle targets, replies, media, edits, and delivery receipts onto the connector | Doolittle retains pairing, session selection, journal/replay, and the raw sender as a startup fallback when the connector is unavailable | Prove live-account hydration and remove the raw outbound fallback only after external-ID, voice, reply, and edit parity is verified |
| Slack | `@elizaos/plugin-slack` registers the official message connector when bot and app tokens are configured; the shared connector adapter owns outbound delivery | The legacy webhook/signing adapter remains as a fallback, and the official plugin currently owns its own inbound lifecycle rather than entering Doolittle's gateway policy pipeline | Keep native Slack opt-in, report connector readiness explicitly, and retain the product inbound/journal boundary until the SDK exposes a typed inbound handoff |
| WhatsApp | `@elizaos/plugin-whatsapp` registers the official Cloud API connector and receives Doolittle outbound messages through the shared connector adapter | Doolittle retains webhook verification, pairing, normalization, sessions, journal/replay, and the raw Cloud API fallback | Keep `WHATSAPP_APP_SECRET` available for official signature validation and remove raw outbound only after live receipt/media parity |
| Signal | `@elizaos/plugin-signal` registers the official connector from an E.164 account and its HTTP or CLI runtime; outbound messages use the shared connector adapter | Doolittle retains its configured command adapter as a compatibility fallback plus the gateway policy/session/journal path | Prefer `SIGNAL_ACCOUNT_NUMBER` with official HTTP/CLI settings; retire `SIGNAL_CLI_COMMAND` after live connector parity and migration guidance |
| Plugin inventory and tools | Eliza's registered actions own executable tool availability through [`service-resolution/tool-inventory.ts`](../packages/agent/src/runtime/native/service-bridge/service-resolution/tool-inventory.ts); `@elizaos/agent/services/registry-client` and the plugin manager own plugin inventory; [`packages/agent/src/services/tools/service.ts`](../packages/agent/src/services/tools/service.ts) supplies control-plane metadata and startup fallback | The product catalog still describes non-action control-plane capabilities that have no direct registered-action equivalent | Keep executable availability action-native and preserve the product catalog only as labeled metadata/fallback |

## Maintainer priorities

1. Keep execution, automation, orchestration, and mutation proof on their SDK-owned contracts; remove compatibility facades when their final callers disappear.
2. Keep the session read projection synchronized from Eliza-owned memories while retaining Doolittle project, fork, search, import/export, and desktop metadata as product behavior.
3. Validate live Discord, Slack, WhatsApp, and Signal connector hydration, then retire each raw outbound fallback independently once receipt, reply, media, and edit parity is proven.
4. Keep the skills hub as generated-skill distribution and an ephemeral
   desktop read projection; catalog lifecycle and mutations belong exclusively
   to the official agent-skills service.
5. Keep every Doolittle-only projection namespaced. Official plugin service
   identifiers such as `shell`, `mcp`, `browser`, and `github` are reserved for their
   SDK owners and may only be adopted through explicit compatibility adapters.
6. Revisit `plugin-todos`, task-coordinator, virtual filesystem, and workflow
   only when they replace a product contract rather than duplicate it. The
   beta.7 workflow artifact is not Node-loadable, and the virtual filesystem is
   isolated state rather than a safe repository workspace.
